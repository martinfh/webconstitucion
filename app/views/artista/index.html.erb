
<!DOCTYPE html>
<html>
  <head>
    <div class="boton_atras">
        <%=link_to (image_tag 'fotos/flecha_atras.png',style:"height:50px"), pagina_inicio_path %>
      </div>
    <title>ARTISTXS</title>
  </head>
  <body>
    <div class="botones_artistas" style="align-content: center;">
      <div class="tab">
        <% @artista.each do |artistum| %>
            <button>
              <%=link_to artistum.nombre, artistum_path(artistum.id),:class=>"links_artistxs"%>
              <p> 
              <%=if user_signed_in?
          link_to 'Eliminar', artistum, method: :destroy, data: { confirm: 'Are you sure?' }
                end%>
              </p>
            </button>
            <% end %>
      </div>
    </div>
              <div> 
              <%=if user_signed_in?
          link_to 'Nuevo Artistx', new_artistum_path
                end%>
              </div>
  </body>
</html>
